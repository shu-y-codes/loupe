#!/bin/sh
# Runs the same lint and tests GitHub Actions will run. Enable once per clone:
#   git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"
uv run ruff check .
uv run pytest
